{
  "name": "functions",
  "scripts": {
    "lint": "eslint --ext .js,.ts ./src",
    "format": "prettier --write ./src --ignore-path ./format/.prettierignore --config ./format/.prettierrc.json",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "yarn build && firebase emulators:start --only functions",
    "shell": "yarn build && firebase functions:shell",
    "start": "yarn shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "prettier-watch": "onchange \"./**/*\" --exclude-path ./format/.prettierignore -- prettier --config ./format/.prettierrc.json --write --ignore-unknown {{changed}}"
  },
  "engines": {
    "node": "16"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^10.0.2",
    "firebase-functions": "^3.18.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.2.1",
    "firebase-functions-test": "^0.2.0",
    "onchange": "^7.1.0",
    "prettier": "^2.7.1",
    "typescript": "^4.5.4"
  },
  "private": true
}
